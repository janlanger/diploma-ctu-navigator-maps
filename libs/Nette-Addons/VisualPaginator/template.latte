{*
 * @version    $Id: template.phtml 2 2009-07-13 20:58:52Z david@grudl.com $
 * @param Paginator $paginator
 * @param array     $steps
 *}

{if $paginator->pageCount > 1}
<div class="paginator">
	{if $paginator->isFirst()}
	<span class="button">« Předchozí</span>
	{else}
	<a href="{if $useSignal}{link page!, 'page' => $paginator->page - 1}{else}{link this, 'page' => $paginator->page - 1}{/if}" class="datagrid-ajax">« Předchozí</a>
	{/if}

	{foreach $steps as $step}
	{if $step == $paginator->page}
		<a href="#" class="current">{$step}</a>
	{else}
		<a href="{if $useSignal}{link page!, 'page' => $step}{else}{link this, 'page' => $step}{/if}" class="number datagrid-ajax">{$step}</a>
	{/if}
	{if $iterator->nextValue > $step + 1}<span>…</span>{/if}
	{/foreach}

	{if $paginator->isLast()}
	<span class="button">Další »</span>
	{else}
	<a href="{if $useSignal}{link page!, 'page' => $paginator->page + 1}{else}{link this, 'page' => $paginator->page + 1}{/if}" class="datagrid-ajax">Další »</a>
	{/if}
</div>
{/if}
